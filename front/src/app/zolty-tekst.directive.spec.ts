import { ZoltyTekstDirective } from './zolty-tekst.directive';

describe('ZoltyTekstDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoltyTekstDirective();
    expect(directive).toBeTruthy();
  });
});
